from django.contrib import admin

'''class CustomUserAdmin(UserAdmin):
    fieldsets = UserAdmin.fieldsets + (
        ('Additional Info', {'fields': ('fullname', 'phone', 'address')}),
    )

admin.site.register(CustomUser, CustomUserAdmin)'''